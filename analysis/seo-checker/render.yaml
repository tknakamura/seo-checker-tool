# render.yaml - Docker最適化版設定
# メルカリSEOチェックツール（全角文字数カウント対応）

services:
  - type: web
    name: seo-checker-tool
    env: docker
    region: oregon
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    autoDeploy: true
    branch: main
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: NODE_OPTIONS
        value: --max-old-space-size=512
    # render.com最適化設定
    buildFilter:
      paths:
        - analysis/seo-checker/**
      ignoredPaths:
        - README.md
        - "*.md"
        - logs/**
        - test/**
        - docs/**